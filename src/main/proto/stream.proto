syntax = "proto3";

package stream;

service StreamService {
  rpc FetchResponseStream (stream Request) returns (stream Response) {}
}

message Request {
  string query = 1;
}

message Response {
  string result = 1;
}